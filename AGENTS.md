# Agent Rules

## Code Changes
- After every code change, provide a summary with git push commands and a descriptive commit message

## Response Style
- AI customer support responses must be plain conversational text — no markdown formatting
- Use git push commands in summaries, not just descriptions
